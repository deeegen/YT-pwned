<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>yuh</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root {
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto,
          Arial;
      }
      body {
        margin: 0;
        padding: 2rem;
        background: #0b1020;
        color: #e7eaf6;
      }
      .card {
        max-width: 720px;
        margin: 0 auto;
        background: #131a33;
        border-radius: 12px;
        padding: 1.25rem;
        box-shadow: 0 6px 24px rgba(0, 0, 0, 0.35);
      }
      h1 {
        margin: 0 0 1rem 0;
        font-size: 1.5rem;
      }
      form {
        display: flex;
        gap: 0.5rem;
        margin-top: 0.5rem;
      }
      input,
      button,
      select {
        font-size: 1rem;
        border-radius: 10px;
        border: none;
        padding: 0.75rem 0.9rem;
      }
      input[type="text"] {
        flex: 1;
        background: #0e1430;
        color: #e7eaf6;
        outline: 1px solid #2a3566;
      }
      button {
        background: #5b82ff;
        color: white;
        cursor: pointer;
      }
      button:hover {
        filter: brightness(1.07);
      }
      .hint {
        color: #aeb8e6;
        font-size: 0.9rem;
      }
      .row {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1rem;
      }
      .split {
        display: grid;
        grid-template-columns: 1fr;
        gap: 0.75rem;
      }
      @media (min-width: 720px) {
        .split {
          grid-template-columns: 1fr 1fr;
        }
      }
      .linkish {
        color: #9ec1ff;
        text-decoration: none;
      }
      .linkish:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <h1>Watch by YouTube ID</h1>
      <div class="hint">
        Paste the 11-character video ID (e.g., <code>dQw4w9WgXcQ</code>).
      </div>
      <form action="/watch" method="POST" autocomplete="off">
        <input
          type="text"
          name="slug"
          pattern="[A-Za-z0-9_-]{11}"
          placeholder="e.g. dQw4w9WgXcQ"
          required
          aria-label="YouTube Video ID"
        />
        <button type="submit">Watch</button>
      </form>

      <hr
        style="border: none; border-top: 1px solid #2a3566; margin: 1.25rem 0"
      />

      <div class="row">
        <div>
          <h1>Search Videos</h1>
          <div class="hint">
            Search YouTube (via yt-dlp) or your downloaded library.
          </div>
          <form class="split" action="/search" method="GET">
            <input
              type="text"
              name="q"
              placeholder="Search videos..."
              required
              aria-label="Search query"
            />
            <div style="display: flex; gap: 0.5rem">
              <select name="source" aria-label="Source">
                <option value="all">All sources</option>
                <option value="youtube">YouTube only</option>
                <option value="local">Local only</option>
              </select>
              <button type="submit">Search</button>
            </div>
          </form>
        </div>
        <div>
          <h1>Your Downloads</h1>
          <div class="hint">Browse everything you've already downloaded.</div>
          <a class="linkish" href="/library">Open Library →</a>
        </div>
      </div>
    </div>
  </body>
</html>
